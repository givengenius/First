name: show-runner

on: workflow_dispatch

jobs:
  info:
    runs-on: ubuntu-latest

    steps:
      - name: show system
        run: |
          uname -a
          cat /etc/os-release
          lscpu
          df -h
